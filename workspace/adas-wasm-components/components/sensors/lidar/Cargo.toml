[package]
name = "adas-lidar_ecu"
version = "0.1.0"
edition = "2021"
description = "LiDAR point cloud processing ECU"
license = "Apache-2.0"


[lib]
crate-type = ["cdylib"]

[dependencies]
wit-bindgen = { workspace = true }

# Configuration for building WASM components
# Profile configuration inherited from workspace
