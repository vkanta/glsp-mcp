[package]
name = "adas-vehicle_control_ecu"
version = "0.1.0"
edition = "2021"
description = "Vehicle actuation control"
license = "Apache-2.0"


[lib]
crate-type = ["cdylib"]

[dependencies]
wit-bindgen = { workspace = true }

# Configuration for building WASM components
# Profile configuration inherited from workspace
