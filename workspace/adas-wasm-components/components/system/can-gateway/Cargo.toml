[package]
name = "adas-can_gateway"
version = "0.1.0"
edition = "2021"
description = "CAN/Ethernet communication"
license = "Apache-2.0"


[lib]
crate-type = ["cdylib"]

[dependencies]
wit-bindgen = { workspace = true }

# Configuration for building WASM components
# Profile configuration inherited from workspace
