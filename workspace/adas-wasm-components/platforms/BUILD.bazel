"""Platform definitions for WASM components."""

package(default_visibility = ["//visibility:public"])

# Platform for WebAssembly Component Model (WASI P2)
platform(
    name = "wasm32-wasip2",
    constraint_values = [
        "@platforms//cpu:wasm32",
        "@platforms//os:wasi",
    ],
)