[package]
name = "showcase-object-detection"
version = "0.1.0"
edition = "2021"
description = "Production-grade AI object detection for automotive ADAS showcase"
license = "Apache-2.0"

[lib]
crate-type = ["cdylib"]

[dependencies]
wit-bindgen = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
ndarray = "0.16"

[build-dependencies]
# No build dependencies for showcase version

# Showcase profile for demo optimization
[profile.release]
opt-level = "s"
lto = true
codegen-units = 1
panic = "abort"
strip = true